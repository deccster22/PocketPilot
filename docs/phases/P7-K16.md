# P7-K16 - Layer 3 Example Wiring Merge

## Why This Phase Happened Now

`P7-K14` and `P7-K15` completed Layer 2 depth for canonical Trade Hub and strategy topics, and `P7-K13` keeps runtime topic-routing drift guarded.
With those dependencies stable, the next bounded step is lightweight Layer 3 cross-reference wiring so existing teaching articles can point to examples without changing runtime behavior or importing new narrative packs.

## Dependencies

- `P7-K13` for runtime catalog sync validation guardrails.
- `P7-K14` for complete Trade Hub Layer 2 baseline before adding example bridges.
- `P7-K15` for complete strategy Layer 2 baseline before adding example bridges.

## What Wiring Was Merged

Compact Layer 3 sections were merged in-place into canonical docs:

1. Strategies: appended or merged `## See examples` for all 9 canonical launch strategies.
2. Concepts: appended or merged `## See how this appears` for payload-covered concepts.
3. Trade Hub: appended or merged `## How this shows up` only for payload-covered topics:
   - `entry-price`
   - `stop-loss-price`
   - `target-price`
   - `reward-risk`

All wiring remains descriptive and optional, and does not add recommendation or dispatch framing.

## Example/Evidence Topic Existence And Handling

The repo already had canonical `market-examples` and `evidence` families, but payload slugs were not all live canonical topic docs.

- Existing canonical example/evidence docs were reused where clear matches existed.
- Non-live payload slugs were mapped without adding runtime dead routes.
- A docs-side map records this wiring state:
  - `docs/knowledge/_meta/LAYER3_EXAMPLE_INTEGRATION_MAP.md`

## How Layered Structure Was Preserved

- Existing Layer 1 and Layer 2 content remains first in each canonical article.
- Layer 3 wiring sections were appended as compact bridges under existing teaching content.
- No duplicate docs, no duplicate families, and no second examples tree were created.

## Runtime Catalog Handling

This repo uses `services/knowledge/knowledgeCatalog.ts` as a runtime topic-detail content artifact.

`P7-K16` updates runtime catalog content only for affected existing runtime topics:

- all 9 canonical strategy topic IDs
- the 4 payload-covered Trade Hub topic IDs

No runtime entries were added for non-live payload example slugs.

## What Was Deliberately Not Changed

- no changes to `docs/governance/CANON.md`
- no changes under `docs/source`
- no app UI layout, navigation, or help-placement changes
- no Strategy Navigator/Preview runtime behavior changes
- no strategy engine logic changes
- no Trade Hub/Risk Tool runtime behavior changes
- no new market-example narrative authoring import
- no visual/example asset import
- no execution or dispatch behavior

## Future Follow-Up

1. Actual example/evidence narrative content import for mapped future slugs, if product scope opens it.
2. Concept Layer 2 deepening.
3. Strategy/concept evidence cleanup pass.
4. Visual/example pilot later, as a separate bounded phase.

## Preview-Check Requirement

Preview check required: **no**.

Reason: this phase adds docs/runtime content wiring only and does not change visible app UI structure, navigation, or layout behavior.
