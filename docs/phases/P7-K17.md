# P7-K17 - Layer 3 Example Standardisation Pass

## Why This Phase Happened Now

`P7-K16` finished Layer 3 cross-reference wiring and `P7-K13` protects runtime catalog sync for explicit runtime-required topics.  
With wiring in place, the next bounded step is standardizing existing example/evidence narrative quality before adding any new broad example coverage or additional link surfacing.

## Dependencies

- `P7-K13` for runtime catalog sync guardrails.
- `P7-K16` for prior Layer 3 wiring and integration-map baseline.

## Audit Findings Used As Source Of Truth

- 6 market-example topics exist.
- 12 evidence topics exist.
- 18 Layer 3-ish topics total.
- 14 KEEP.
- 4 UPGRADE.
- 0 HOLD BACK.
- Main problem: standardization consistency (shape, behavior-first naming, explicit mapping), not missing broad content volume.

## What Was Standardized

All 18 in-scope canonical topic docs under:

- `docs/knowledge/market-examples/**`
- `docs/knowledge/evidence/**`

Each standardized topic now follows one consistent structure:

- `## Context`
- `## Setup`
- `## What happened`
- `## What to take from it`
- `## Where this fits` with explicit `Strategies` and `Concepts` lists

Visible framing was shifted from worked-trade phrasing to behavior-first phrasing (continuation, acceptance, caution, failed-hold, danger-confluence).

## UPGRADE Topics Sharpened

The four audit-marked UPGRADE topics were rewritten with targeted clarity:

- `momentum-pulse-bitcoin-june-to-august-2022-caution`:
  speed versus structure distinction tightened.
- `range-trader-bitcoin-august-2020-failed-escape-caution`:
  range-context and transition-pressure framing made explicit.
- `reversion-bounce-bitcoin-june-to-august-2022-caution`:
  relief bounce versus durable repair distinction tightened.
- `confluence-alignment-terra-exit-spiral-caution`:
  reframed around danger confluence and stacked risk, not opportunity confluence.

## Layer 3 Integration Map Handling

`docs/knowledge/_meta/LAYER3_EXAMPLE_INTEGRATION_MAP.md` already existed before this pass and was updated in place (not duplicated).

Updated map now captures:

- live standardized topic coverage
- KEEP/UPGRADE status
- strategy/concept mappings
- missing coverage gaps
- future not-yet-created topics
- explicit not-yet-surface guardrails

## Runtime Catalog Handling

This repo uses `services/knowledge/knowledgeCatalog.ts` as runtime topic-detail content.  
For this phase, no runtime entries were changed because these 18 market-example/evidence topic IDs are still docs-side topics and were not part of current runtime catalog route scope from `P7-K16`.  
No new runtime routes were introduced for future/non-live topics.

## What Was Deliberately Not Changed

- no `docs/governance/CANON.md` edits
- no `docs/source/**` edits
- no Strategy Navigator/Preview runtime behavior changes
- no strategy engine logic changes
- no Trade Hub/Risk Tool layout/copy/help-placement changes
- no new help terms
- no Trade Hub example-link surfacing expansion
- no broad new example-topic authoring outside existing 18 in-scope topics

## Remaining Gaps

- Range Trader still needs a clean played-out example topic (future).
- Reversion Bounce still needs a clean played-out example topic (future).
- Candle Signals still lacks a proper Layer 3 evidence topic.
- Fibonacci Zones still lacks a proper Layer 3 evidence topic.

## Future Follow-Up

- Add missing range/reversion played-out examples in a later bounded phase.
- Add Candle Signals and Fibonacci Zones evidence topics in a later bounded phase.
- Add concept-recognition sections in a later pass if approved.
- Revisit Trade Hub example links only after example set stability is accepted.
- Consider visual/example pilot as a separate future phase.
