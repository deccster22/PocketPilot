# P8-I12 - Since Last Checked Archive / History Continuity

## Why This Phase Happened Now
P6-A7 added the calm Snapshot Since Last Checked section.
P6-A8 refined the same section so it clears after view through one service-owned viewed-state seam.
P8-I3 already established one deeper interpreted Insights archive shelf.

That made the next safe step clear:

- keep Snapshot front-of-house compact and clear-after-view
- keep continuity account-scoped and meaningful-change-only
- add one service-owned bridge so cleared Snapshot continuity remains available in deeper history/archive paths

P8-I12 happens now because the product already has stable Snapshot viewed-state and Insights archive foundations, so continuity can deepen without introducing inbox, badge, push, or feed behavior.

## What P8-I12 Added
P8-I12 adds four narrow refinements:

1. One canonical Since Last Checked archive continuity contract family in `services/insights/types.ts`.
2. One service-owned continuity composition seam in `services/insights/createSinceLastCheckedArchiveVM.ts`.
3. One canonical continuity fetch seam in `services/insights/fetchSinceLastCheckedArchiveVM.ts`.
4. One bounded deeper-surface handoff through the existing Insights archive path so `InsightsDetailScreen` renders prepared continuity only.

The deeper continuity read path is now:

`EventLedger -> EventLedgerQueries -> Snapshot viewed boundary -> createSinceLastCheckedArchiveVM -> fetchSinceLastCheckedArchiveVM -> fetchInsightsArchiveVM -> InsightsDetailScreen`

## Preserved Behavior
P8-I12 explicitly preserves:

- Snapshot Since Last Checked cap at 3 items
- Snapshot clear-after-view behavior from P6-A8
- account-scoped service ownership for selection and filtering
- app render-only posture (no local continuity derivation)
- calm interpreted copy without turning into event rows

## What P8-I12 Deliberately Did Not Add
P8-I12 does not add:

- push notifications
- background polling
- inbox or unread badge semantics
- notification center behavior
- raw event feed UI
- generic history-browser expansion
- manual archive-management controls
- urgency or performance-theatre language

## What Future P8 Work Can Build On
Later P8 work can safely extend this seam with:

- richer continuity summaries that still stay prepared and service-owned
- additional interpreted continuity grouping where it remains calm and account-scoped
- deeper archive refinements that continue to avoid inbox/feed platformization

## Recommendation Review
- Adopt now: One canonical Since Last Checked archive/history continuity seam so Snapshot can clear after view while deeper continuity remains available in a prepared account-scoped path.
- Add to backlog: Richer continuity grouping and optional archive refinements that still reuse the same service seam without widening scope.
- Decline for now: Inbox-style unread systems, badge mechanics, push/background monitoring, notification center workflows, and raw feed expansion.
