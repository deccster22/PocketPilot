# P7-K14 - Trade Hub Layer 2 Knowledge Merge

## Why This Phase Happened Now

`P7-K13` closed the immediate runtime topic-routing drift risk for first-rollout Trade Hub help topics.  
With that guard in place, the next bounded step was to deepen the same canonical Trade Hub articles in-place instead of adding new surfaces, new terms, or new UI wiring.

This phase completes Trade Hub Layer 2 coverage for the 14 canonical Trade Hub topics while keeping the same calm, non-dispatching posture.

## Dependencies

- `PX-KI4` for canonical Trade Hub family import and register baseline.
- `P7-K12` for first bounded Trade Hub help rollout scope and term constraints.
- `P7-K13` for runtime catalog sync validation guardrails.

## What Layer 2 Content Was Merged

Layer 2 deeper-thinking sections were merged into the existing canonical docs for:

1. `entry-price`
2. `stop-loss-price`
3. `target-price`
4. `prepared-planning-levels`
5. `risk-amount`
6. `risk-percent`
7. `position-size`
8. `reward-risk`
9. `manual-override`
10. `guardrails`
11. `risk-limit-per-trade`
12. `daily-loss-threshold`
13. `cooldown-after-loss`
14. `confirmation-shell-and-non-dispatch-boundary`

Each article now keeps beginner Layer 1 content first and appends Layer 2 sections with this pattern:

- `How to think about it`
- `How it behaves`
- `What tends to go wrong`
- `What good looks like`
- `What it connects to`

## How Layered Structure Was Preserved

- Existing Layer 1 sections (`What this means`, `Why it matters`, `How PocketPilot frames it`, `Common misunderstanding`) remain at the top of each article.
- Layer 2 is appended below Layer 1 under a clear `Layer 2: Deeper thinking` boundary section.
- Tone remains calm, optional, and non-directive; content is framing-oriented, not advice-oriented.

## Runtime Catalog Handling

This repo uses `services/knowledge/knowledgeCatalog.ts` as a runtime topic-content artifact for Knowledge Topic detail routes.

Accordingly, `P7-K14` updates runtime catalog content for the same 14 `trade-hub-*` topic IDs so runtime detail content reflects the merged Layer 1 + Layer 2 articles.

Deliberately unchanged:

- no expansion of Trade Hub help-term wiring scope
- no change to `P7-K13` validation behavior or enforcement semantics
- no runtime routing ownership change in `app/`

## What Was Deliberately Not Changed

- no changes to `docs/governance/CANON.md`
- no changes under `docs/source`
- no app UI copy/layout or help-placement changes
- no new help terms
- no changes to risk math, guardrail semantics, readiness/submission/handoff behavior
- no execution/dispatch logic; PocketPilot remains non-dispatching

## Future Follow-Up

1. Trade Hub Layer 3 examples integration (bounded, optional, text-first).
2. Strategy Layer 2 deepening pass (progressive layering, no advice drift).
3. Concept Layer 2 deepening pass (progressive layering, no glossary noise expansion).
4. Visual/example pilot only after a bounded text-layer review.

## Preview-Check Requirement

Preview check required: **no**.

Reason: this phase is docs/runtime content layering only, with no visible UI structure, navigation, or placement changes.
