# P7-K15 - Strategy Layer 2 Knowledge Merge

## Why This Phase Happened Now

`P7-K14` completed Trade Hub Layer 2 content and `P7-K13` keeps runtime topic routing drift guarded.
With those in place, the next bounded step is to complete Layer 2 depth for canonical launch strategy topics using the same in-place layered-doc approach.

## Dependencies

- `PX-KI5` for the existing strategy progressive-layer baseline and canonical strategy docs.
- `P7-K13` for runtime catalog sync validation guardrails.
- `P7-K14` for the immediately prior Layer 2 merge pattern and runtime parity discipline.

## What Layer 2 Content Was Merged

Layer 2 deeper-behaviour content was merged into the canonical strategy docs for:

1. `buy-the-dip`
2. `range-trader`
3. `reversion-bounce`
4. `trend-follow`
5. `breakout-watcher`
6. `momentum-pulse`
7. `candle-signals`
8. `fibonacci-zones`
9. `confluence-alignment`

Each topic adds:

- `How it behaves`
- `What strengthens it`
- `What weakens it`
- `How it tends to fail`
- `How it connects to planning`
- `What to watch for`

under a clear boundary:

- `Layer 2: Deeper behaviour`

## How Layered Structure Was Preserved

- Existing Layer 1 beginner content remains at the top of each strategy article.
- Existing richer deeper content from prior phases was preserved.
- Layer 2 content was appended in-place beneath existing content in the same canonical article file.
- No duplicate strategy docs, folders, or alternative strategy families were created.

## Copy and Terminology Polish Applied

Bounded wording polish was applied to satisfy phase guardrails:

- `buy-the-dip`: softened to “where the dip idea may no longer be valid”.
- `range-trader`: replaced “repeated trades” framing with “repeated plans”.
- `confluence-alignment`: softened to “does not make the outcome certain”.
- `candle-signals`: removed “guarantee” wording from misunderstanding copy.

## Runtime Catalog Handling

This repo uses `services/knowledge/knowledgeCatalog.ts` as a runtime topic-detail artifact.

`P7-K15` therefore updates runtime strategy topic-detail content (`content` + `sections`) for the same 9 canonical strategy topic IDs so Layer 2 appears in app topic detail routes.

Deliberately unchanged:

- no Strategy Navigator or Strategy Preview behavior changes
- no help-term wiring expansion
- no `P7-K13` validation behavior weakening

## What Was Deliberately Not Changed

- no changes to `docs/governance/CANON.md`
- no changes under `docs/source`
- no app UI layout or copy-placement changes
- no strategy engine logic changes
- no Trade Hub/Risk Tool UI/help placement changes
- no Layer 3 examples or visual/example packs imported
- no execution/dispatch behavior introduced

## Future Follow-Up

1. Strategy Layer 3 examples integration (bounded, optional, non-directive).
2. Concept Layer 2 deepening.
3. Evidence cleanup pass.
4. Visual/example pilot later (separate scoped phase).

## Preview-Check Requirement

Preview check required: **no**.

Reason: this phase updates docs/runtime topic content only and does not change visible app hierarchy, navigation, or UI placement rules.
